# 2026-07-17

## n8n owner password reset

- Owner login password (account `shepardburner@gmail.com`, re-created during the
  2026-05-29 SQLite→Postgres migration) was forgotten. It is stored only as a
  bcrypt hash in `n8n-postgres`, so it could not be recovered.
- Ran `docker exec n8n n8n user-management:reset` — puts the instance back in
  setup mode while keeping all workflows, credentials, and executions in
  Postgres.
- Restarted the `n8n` container; verified the `user` table now has a single
  blank owner row (NULL password) and the web UI responds 200.
- Next visit to the n8n web UI shows the owner-setup screen to create the new
  login. No compose/config files changed.
