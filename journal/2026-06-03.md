# 2026-06-03

## Hermes: enable n8n workflow create/edit

Owner wanted Hermes to create and edit n8n workflows (not just read). The MCP bridge has
no create/edit tool, so:

- Whitelisted `activate_workflow` + `deactivate_workflow` in `mcp_servers.n8n.tools.include`
  (14 n8n tools now register; `container_logs` still excluded).
- Create/edit done via the **n8n REST API** (key in `/opt/data/.env` has full CRUD —
  verified by creating + deleting a throwaway workflow). Added a standing instruction in
  `/opt/data/memories/USER.md` so the agent knows the endpoints AND the guardrails:
  confirm before create/edit, new workflows created inactive, never DELETE without an
  explicit named request, don't touch the 5 production workflows unless named.
- Caveat: community n8n API keys are full-access (no read-only scope) — so the key can
  also delete. Protection = Telegram allowlist (owner-only) + USER.md guardrails.

Docs: [`docs/17-hermes.md`](../docs/17-hermes.md) → "Tool scope".

## Hermes: request movies/TV by text (Radarr + Sonarr)

Wired Hermes into the media stack so the owner can ask for downloads in plain language.

- Stored Radarr (`:7878`) + Sonarr (`:8989`) URLs/keys in `/opt/data/.env` (keys read from
  `/mnt/drive1/appdata/{radarr,sonarr}/config.xml`). Verified both APIs (Radarr v6,
  Sonarr v4) and the lookup path (`/movie/lookup?term=dune part two` → Dune: Part Two 2024).
- Added a media section to `/opt/data/memories/USER.md`: lookup → confirm → POST add+search,
  with defaults (Radarr profile 4 HD-1080p, root /mnt/drive1/movies; Sonarr profile 7
  WEB-1080p, root /mnt/drive2/shows) and guardrails (confirm title, one at a time, no
  deletes, no settings changes, report queue after). No 4K profile exists — Hermes told to
  offer 1080p.
- Flow respects the existing pipeline: Radarr/Sonarr → Prowlarr → qBittorrent (Gluetun) →
  hardlink import. Hermes is just the natural-language trigger.

Docs: [`docs/17-hermes.md`](../docs/17-hermes.md) → "Media downloads".

## Lidarr (music *arr) installed + Hermes wired; Soularr deferred

Owner wanted a music *arr (like slskd, but slskd = Soulseek/P2P, so the torrent answer is
Lidarr). Installed Lidarr and wired Hermes to it.

- **Lidarr** (`lscr.io/linuxserver/lidarr`, `:8686`) added via CasaOS
  ([`compose/lidarr.yml`](../compose/lidarr.yml)); mirrors radarr/sonarr (PUID/PGID 1000,
  /mnt/drive1 1:1 for hardlinks). Full writeup: [`docs/18-lidarr.md`](../docs/18-lidarr.md).
- Configured via API: root folder `/mnt/drive1/Downloads/Soulseek` (the existing library;
  **chown -R 1000:1000** so Lidarr can write — owner-approved), qBittorrent client
  (mirrored from Radarr, category `lidarr`, test OK), registered as a Prowlarr Application
  (fullSync → 3 indexers pushed). Health clean.
- **Hermes**: `LIDARR_*` creds in `/opt/data/.env` + a Music section in
  `/opt/data/memories/USER.md` (lookup→confirm→add artist / monitor album + AlbumSearch;
  guardrails: confirm, one at a time, no deletes/settings changes).
- **Soularr DEFERRED**: it needs slskd, but `gluetun-slskd`'s WireGuard VPN has been dead
  ~8 weeks (handshake completes, no traffic; slskd web `HTTP 000`). Torrent music works
  now via the healthy `gluetun-qbit`. Fixing the slskd VPN (needs owner's WireGuard
  config; kill-switch/IP-sensitive) is a separate follow-up. plexamp left alone per owner.
