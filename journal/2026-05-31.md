# 2026-05-31 — n8n: Todoist capture bot + Things migration

## Summary

Built a two-way **Todoist capture bot** (text the ASJ Noti bot → Gemini cleans it
→ Todoist task) and a one-time **Things 3 → Todoist importer**. Also hardened the
capture flow after it dropped messages in real use.

## Changes

- **Todo Capture → Todoist (via Gemini)** (workflow `r5gc1hgtCyimoN5v`):
  Telegram polling → `gemini-3.5-flash` extracts {title, due, label, priority}
  → Todoist `POST /api/v1/tasks` → ✅ reply.
  - Uses **outbound `getUpdates` polling** (not a Telegram Trigger) because
    n8n.asj.media is LAN-only and Telegram can't reach an inbound webhook.
  - Added **Gemini API key** (HTTP Header `x-goog-api-key`) and **Todoist API**
    (Bearer) credentials.
- **Things → Todoist Import** (workflow `EsEMth8aREpDrYcn`): webhook receives an
  Apple-Shortcut payload of open to-dos and creates them in Todoist, deduped,
  with Things Area/Project preserved as `Things_*` labels.
  - Migrated **40 tasks**; later organized into Todoist **Sections** (Kord, Blog,
    Church, Coding, Routines) and deleted the 14 guitar/theory "music" tasks.
- Sanitized exports: `configs/n8n-workflow-todo-capture-todoist.example.json`,
  `configs/n8n-workflow-things-import.example.json`.

## Why

Wanted to capture todos by texting the existing bot, and to move off Things 3
(Apple-only, no API) onto Todoist (full REST API, iOS widgets).

## Gotchas (all hit live, all fixed)

- **Gemini model:** the consumer Gemini *subscription* has no API. Must use an
  **API key** from AI Studio (free tier). Model is `gemini-3.5-flash` (newer than
  my assumptions — verified against the live `/models` list).
- **n8n expression `}}` collision:** building the Gemini JSON body inline as
  `={{ JSON.stringify({...}) }}` breaks because nested `}}` ends the expression.
  Fix: build the body string in a Code node, reference it as `={{ $json.geminiBody }}`.
- **Email→Things dead end:** "Mail to Things" only supports title+notes — no
  dates/projects/tags. Pivoted to Todoist for structured fields.
- **Proton SMTP:** only works on paid plans via SMTP submission tokens
  (`smtp.protonmail.ch:587`); free Proton has no SMTP. (Used briefly for the
  Things email experiment, since dropped in favor of Todoist.)
- **Lost messages (6 sent, 1 captured):** the offset was advanced when messages
  were *read*, before tasks were created — so a Gemini rate-limit (429) on a burst
  silently dropped 5. **Fix:** process **one message per poll** and advance the
  offset only in a final **Mark read** node *after* the Todoist task is created.
  Now nothing is lost and nothing is duplicated.
- **404 Project not found:** capture had hardcoded Home/Education project IDs;
  those Todoist projects were later deleted. **Fix:** capture into Inbox and apply
  the category as a **label** (auto-creates, never 404s).
- **Execution history is useless for a 1-min poller** — successful runs are pruned
  (last 20 kept), so message-processing runs roll off within ~20 min. Debugged via
  the Telegram offset + Todoist state instead.

## Files touched

- `configs/n8n-workflow-todo-capture-todoist.example.json` (new, sanitized)
- `configs/n8n-workflow-things-import.example.json` (new, sanitized)
- `docs/15-n8n.md` (Todoist capture + Things import sections)
- `journal/2026-05-31.md` (this entry)

## What's next

- Optional: let the capture bot drain a small batch per poll (faster than 1/min)
  while keeping the mark-read-after-success guarantee.
- Optional: route captured todos into Todoist Sections, not just labels.
- Still open from 2026-05-29: clear **drive2 (100% full)**; add n8n-postgres to
  `scripts/backup.sh`.

---

# 2026-05-31 — FlacPlayer TODO.md → Todoist mirror

## Summary

Mirrored the open tasks from the private `AndresASJ/FlacPlayer` repo's `TODO.md`
into Todoist (label `FlacPlayer`), one-way, file as source of truth. 77 tasks
imported; hourly cron keeps it current.

## Changes

- `scripts/flacplayer-todo-sync.py` → `/usr/local/bin/` — fetches `TODO.md` via
  `gh` (reuses the box's GitHub auth, no new token), parses open `- [ ]` items
  (title = first sentence, rest → description, tier prefixed), creates missing
  Todoist tasks and completes ones no longer open in the file.
- Todoist token stored at `/root/.config/flac-sync/todoist.token` (chmod 600).
- Cron (root): `7 * * * *` (hourly).
- 77 tasks created under the `FlacPlayer` label; sync verified idempotent (0/0
  on re-run).

## Why

User wanted their FlacPlayer dev TODO synced with Todoist. TODO.md is a 600-line
tiered design doc — two-way sync would mangle it, so one-way mirror (file = truth)
is the safe, useful choice.

## Gotchas

- **Todoist strips `@word`/`#word` from task `content`** (parses them as label /
  project syntax) — e.g. "Drop unbounded @Query fetches" stored as "Drop unbounded
  fetches" + a junk `Query` label. Caused endless create/complete churn until
  caught. Fix: **backslash-escape** `@`/`#` in titles (zero-width space did NOT
  work; Todoist still parsed it).
- Matching by title text is therefore unreliable — switched to a stable
  **`fpid:<md5>` marker in the description** (descriptions aren't parsed). Now
  idempotent.
- Todoist `GET /tasks` **paginates** (50/page via `next_cursor`); the first
  delete pass missed ~27 tasks until pagination was handled.

## Files touched

- `scripts/flacplayer-todo-sync.py` (new)
- `docs/15-n8n.md` (FlacPlayer mirror section)
- `journal/2026-05-31.md` (this entry)

## What's next

- Optional: route FlacPlayer tasks into a Todoist Section, not just a label.
- If two-way is ever truly wanted, embed Todoist task IDs back into TODO.md and
  reconcile — but the doc's structure makes that risky; not recommended.

---

# 2026-05-31 — Smart capture: iCloud contacts → tap-to-call/text

## Summary

Made the Todoist capture bot intent-aware: "call hendrix later" now creates a task
with tap-to-call / tap-to-text links, using a name looked up live from iCloud
contacts. Also moved all 77 FlacPlayer tasks into the Inbox **Kord** section.

## Changes

- **iCloud contacts via CardDAV** → `scripts/contacts-sync.py` (also `/usr/local/bin/`):
  discovers principal → addressbook-home-set → `card/`, REPORTs vCards, parses
  FN+TEL, writes a `contacts` table into the n8n Postgres DB via `docker exec`.
  Apple app-specific password stored at `/mnt/drive1/AppData/contacts-sync/icloud.cred`
  (chmod 600). Initial sync: 38 vCards → 32 name/number rows.
- **Capture flow now intent-aware**: Gemini returns `action`+`person`; a Postgres
  **Lookup contact** node (scalar-subquery, always 1 row) finds the number; a Build
  step injects `📞 [Call](tel:…) • 💬 [Text](sms:…)` into the task description.
  Created an n8n **Postgres credential** (to its own DB). Verified end-to-end:
  "call hendrix later" → task "Call Hendrix" with working tel:/sms: links.
- **FlacPlayer tasks → Kord section**: moved all 77; sync script now sets
  `section_id` on create so new ones land in Kord too.

## Why

User wanted "smart" todos — recognize a person, match the phonebook, attach a
call/text action. Contacts live in iCloud (no API from the server), so CardDAV
(app-specific password) is the reachable route.

## Gotchas

- iCloud CardDAV uses a **partition host** (e.g. `p141-contacts.icloud.com`) returned
  in `addressbook-home-set` — the script discovers it each run rather than hardcoding.
- The n8n-postgres container has **no published host port**, so the contacts sync
  writes via `docker exec n8n-postgres psql` (host can't reach it over TCP).
- Postgres lookup must **always return a row** (scalar subqueries) or the n8n chain
  breaks on no-match; empty `person` is guarded so `LIKE '%%'` can't match everyone.

## Files touched

- `scripts/contacts-sync.py` (new)
- `scripts/flacplayer-todo-sync.py` (Kord section on create)
- `configs/n8n-workflow-todo-capture-todoist.example.json` (re-export: intent + lookup)
- `docs/15-n8n.md` (smart-capture / contacts section)
- `journal/2026-05-31.md` (this entry)

## What's next

- Daily cron for `contacts-sync.py` (keep phonebook fresh).
- More intents: `email → mailto:`, `directions → maps`, `meet → calendar`.

---

# 2026-05-31 — drive2 rebalance (storage)

## What

`drive2` had filled to 100% (≈7 GB free, a genuine write-failure risk). All 1.8 TB
of it was `/mnt/drive2/shows`. Rebalanced onto the other media drives and cleaned
up one duplicate.

- Deleted duplicate **Prehistoric Planet (2022) S02** raw-release folder (11 GB) —
  byte-for-byte identical to the already-imported S02 in the managed folder.
- Moved via the **Sonarr API** (`PUT /series/<id>?moveFiles=true`, which relocates
  files *and* updates the DB path atomically):
  - **South Park** 246 GB → `drive3`
  - **Rick and Morty** 151 GB → `drive4`
  - **Curb Your Enthusiasm** 100 GB → `drive4`

Result: **drive2 100%→72%** (508 GB free), drive3 84% (296 GB), drive4 78% (394 GB).
All three series verified at their new paths with full file counts (198 / 79 / 120)
and the old `drive2` folders confirmed removed.

## Why

drive2 at 100% blocks any new imports landing there and risks corruption. Other
media drives had plenty of headroom (drive3 542 GB, drive4 646 GB free).

## Gotchas

- **Never `mv` a show across drives by hand** — Sonarr tracks each series by path
  and would orphan it. The API move with `moveFiles=true` keeps the DB consistent.
- Verified **zero hardlinks** on drive2 first (`find -links +1` → 0). Downloads live
  on drive1, so the shows were already standalone copies — cross-drive move is a
  clean copy+delete with no storage doubling and no broken links.
- Sonarr **serializes** `MoveSeries` commands, so queuing all three didn't thrash
  drive2's read head; copies ran ~550 MB/s NVMe→NVMe.
- Left the lowercase `Jujutsu Kaisen` folder (22 GB) in place — it is *not* a clean
  duplicate of managed `JUJUTSU KAISEN` (it has S01 + the JJK 0 movie the managed
  series lacks). Reconciling it into Sonarr is a future cleanup.

## Files touched

- `docs/02-storage.md` (corrected drive table, added multi-drive root-folder +
  rebalance-procedure section)
- `journal/2026-05-31.md` (this entry)

## What's next

- Still open from the n8n handoff: add `n8n-postgres` to `scripts/backup.sh`.
- Optional: reconcile the stray `Jujutsu Kaisen` folder into the managed series.

---

# 2026-05-31 — Recyclarr fix, qBit seeding, R&M re-grab

## What

**Recyclarr was silently broken.** Sonarr's container IP had drifted (now
`172.17.0.7`; config pointed at the stale `.9`), so every `recyclarr sync` failed
with "Connection failed" — unnoticed for weeks. Live profile had therefore drifted.
- Fixed `base_url` → host LAN IP `192.168.50.178` (stable across restarts) for both
  Sonarr + Radarr.
- `recyclarr state repair --adopt` (33 + 1 entries) so it re-owned the existing CFs.
- **x265 (HD) was scored −10000** (TRaSH default) which, with `minFormatScore 0`,
  *rejects all 1080p x265* — the exact opposite of the config's "prefer x265" intent.
  Overrode to **+100** (eligible + mild tiebreak; WEB Tiers still dominate). Synced ✓.

**qBit seeding review.** 63 torrents / 468 GB on disk, `GlobalMaxRatio=-1` (seed
forever, never cleaned). Split: private (ratio/reward matters) vs public (no reward).
**Zero hardlinks anywhere (0/1088 files)** — every import is a standalone copy, so the
intended hardlink setup is NOT working and storage is doubled. Agreed policy: private
seed to ratio 1.0 / 14 d then clean; public token-seed then clean.

**INCIDENT (own goal):** applying qBit per-torrent share limits — even with action
= pause — instantly flagged already-satisfied torrents as "seed-complete", and with
Sonarr/Radarr **Completed Download Handling enabled**, the *arr auto-removed +
deleted **32 imported torrents** (~120 GB of download copies) before I reverted.
- **Library 100% intact** — verified every removed title still has its files (no
  hardlinks = library holds separate copies; *arr only removes what it imported).
- Of 8 private removed, 5 had high ratios (rewards banked); 3 were low-ratio but
  seeded 23 d. 20 private torrents still seeding, untouched.
- Reverted all share limits to unlimited; qBit stable. Lesson saved to memory.
- Deferred-cleanup reminder cancelled (the batch it targeted is already gone).

**Rick and Morty re-grab** (S01–S06 were Bluray Remux, ~140 GB). Explicitly grabbed
the AMZN WEB-DL H.264 season packs (~7 GB each, score 1775) so Sonarr replaces the
Remux on import. Target ~140 GB → ~42 GB.

## Why

Files were "massive" because the library held legacy Bluray x264/Remux, not the
WEB x265 the config *thought* it preferred — and Recyclarr hadn't been able to
correct anything since the IP drift.

## Gotchas

- **Never set qBit share/seed limits on live torrents while *arr Completed Download
  Handling is on** — it cascades into auto-remove+delete of every torrent already
  past the limit. Drive cleanup via `scripts/qbit-cleanup.py` (direct, import-verified
  qBit delete) instead.
- Recyclarr `base_url` must be the host LAN IP, never the Docker container IP
  (drifts on restart → silent sync failure).
- x265 (HD) default score −10000 blocks 1080p x265; needs a positive override.
- qBit 5.x: resume = `/api/v2/torrents/start` (not `/resume`); API reachable only
  via `docker exec qbittorrent curl 127.0.0.1:8090` (host gets "Forbidden").

## Files touched

- `configs/recyclarr.yml.example` (LAN-IP base_url, x265 +100 override)
- `docs/12-recyclarr.md` (x265 gotcha, LAN-IP guidance)
- `scripts/qbit-cleanup.py` (new — import-verified qBit cleanup, report/apply modes)
- `journal/2026-05-31.md` (this entry)

## What's next

- 🚨 **gluetun-qbit VPN is DOWN** (discovered while checking the R&M grabs). WireGuard
  connects to the Proton endpoint (146.70.202.98) but passes no traffic; gluetun
  restart-loops on failed healthcheck (`lookup github.com: i/o timeout`). qBit is
  `firewalled`, `listen_port=0`, 0 peers — **nothing downloads or seeds**, and the
  `*/5` port cron logs "No port file". Likely dead/changed Proton endpoint or stale
  WireGuard config → needs a fresh Proton WG config (their account). The 6 R&M packs
  are queued and will start once the tunnel is restored. (Seeding/rewards have likely
  been dead for ~weeks — gluetun "unhealthy" 3 weeks.)
- ⚠️ Fix the broken hardlinking (downloads on drive1, library on drive2/3/4 = cross-
  drive copies) — biggest structural storage win.
- Verify R&M WEB packs import + replace Remux cleanly once VPN is back; reclaim confirmed.
- Seinfeld x265 re-grab still pending (user pre-approved the x265 Blu-ray route).
